const {User, Board,  Cheese} = require('./models')
const { describe } = require('./models/board.model')
const db = require("./db/db")

describe("testing that tables can be created and updated", ()=>{
    test("creating the tables", async () => {
        
    })
})